# Phase 16b — gateway-service 컨테이너 오버라이드.
#
# 비어 있는 것이 정상이다. 게이트웨이가 컨테이너에서 달라져야 하는 값은 **다운스트림 주소뿐**이고,
# 그건 common.yml 의 `services.*` 가 이미 덮는다(라우트가 `${services.order}` 로 참조).
# 파일 자체를 두는 이유는 마운트 구조를 6개 서비스가 똑같이 유지하기 위해서다.
{}
