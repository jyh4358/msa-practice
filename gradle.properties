org.gradle.caching=true
org.gradle.parallel=true
# configuration-cache 는 끈다: io.spring.dependency-management 플러그인이 아직 비호환
# (legacy configuration 직렬화 에러). Boot 3.5의 표준 의존성 관리 방식을 쓰기 위함.
org.gradle.jvmargs=-Xmx1g -Dfile.encoding=UTF-8
