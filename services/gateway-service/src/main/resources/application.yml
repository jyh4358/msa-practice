spring:
  application:
    name: gateway-service
  config:
    # 라우트·jwk-set-uri·eureka 는 config-service에서 온다.
    import: "optional:configserver:http://localhost:8888"

server:
  port: 8000
  shutdown: graceful
